type Company {
  id: String!
  createAt: String!
  updateAt: String!
  companyLogo: String!
  companyName: String!
  companyOwner: User!
  companyAdress: CompanyAdress
  # computed
  isOwned: Boolean!
  isManger: Boolean!
  companyManager: [User]!
  connectedCompany: [Company]
  connectedCompanyCount: Int!
  connectingCompany: [Company]
  connectingCompanyCount: Int!
}
